# Releases

This folder is the default durable output target for `release-notes.md` when no external release system is configured.

Suggested naming:

- `YYYY-MM-DD-release-notes.md`

Keep release notes:

- user-facing
- grouped by outcome
- explicit about migrations or breaking changes
- grounded in real shipped signals

