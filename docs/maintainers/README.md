# Maintainer Docs

This is the curated technical context layer for humans and Claude.

Use this folder for information that is worth preserving across sessions:

1. start with `overview/system-overview.md`
2. keep local setup in `development/`
3. add service boundaries, runbooks, and release notes as the project grows

When these docs conflict with code, trust the code and update the docs.

